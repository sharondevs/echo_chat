<!--
    Azure API Management Policy for Echo-CHAT RAG API
    This policy handles CORS, authentication, rate limiting, and request/response transformation
-->
<policies>
    <inbound>
        <!-- Remove any existing CORS headers to avoid conflicts -->
        <set-header name="Access-Control-Allow-Origin" exists-action="delete" />
        <set-header name="Access-Control-Allow-Methods" exists-action="delete" />
        <set-header name="Access-Control-Allow-Headers" exists-action="delete" />
        
        <!-- Enable CORS for web applications -->
        <cors allow-credentials="false">
            <allowed-origins>
                <origin>*</origin>
            </allowed-origins>
            <allowed-methods preflight-result-max-age="300">
                <method>GET</method>
                <method>POST</method>
                <method>PUT</method>
                <method>DELETE</method>
                <method>OPTIONS</method>
            </allowed-methods>
            <allowed-headers>
                <header>*</header>
            </allowed-headers>
        </cors>
        
        <!-- Rate limiting per subscription -->
        <rate-limit-by-key calls="100" renewal-period="60" increment-condition="@(context.Response.StatusCode >= 200 && context.Response.StatusCode < 300)" counter-key="@(context.Subscription?.Key ?? "anonymous")" />
        
        <!-- Quota limiting per subscription (daily) -->
        <quota-by-key calls="10000" renewal-period="86400" increment-condition="@(context.Response.StatusCode >= 200 && context.Response.StatusCode < 300)" counter-key="@(context.Subscription?.Key ?? "anonymous")" />
        
        <!-- Set backend service URL -->
        <set-backend-service url="{{function-app-url}}" />
        
        <!-- Add request tracking headers -->
        <set-header name="X-Request-ID" exists-action="override">
            <value>@(Guid.NewGuid().ToString())</value>
        </set-header>
        
        <!-- Forward client IP -->
        <set-header name="X-Forwarded-For" exists-action="override">
            <value>@(context.Request.IpAddress)</value>
        </set-header>
        
        <!-- Add API version header -->
        <set-header name="X-API-Version" exists-action="override">
            <value>1.0</value>
        </set-header>
    </inbound>
    
    <backend>
        <!-- Configure retry policy for backend calls -->
        <retry condition="@(context.Response.StatusCode >= 500)" count="3" interval="2" />
    </backend>
    
    <outbound>
        <!-- Remove backend-specific headers -->
        <set-header name="Server" exists-action="delete" />
        <set-header name="X-Powered-By" exists-action="delete" />
        
        <!-- Add response headers -->
        <set-header name="X-Content-Type-Options" exists-action="override">
            <value>nosniff</value>
        </set-header>
        
        <set-header name="X-Frame-Options" exists-action="override">
            <value>DENY</value>
        </set-header>
        
        <!-- Cache control for health endpoint -->
        <choose>
            <when condition="@(context.Request.Url.Path.Contains("/health"))">
                <set-header name="Cache-Control" exists-action="override">
                    <value>no-cache, no-store, must-revalidate</value>
                </set-header>
            </when>
        </choose>
    </outbound>
    
    <on-error>
        <!-- Log errors -->
        <trace source="echo-chat-api-error">
            <message>@{
                return new JObject(
                    new JProperty("timestamp", DateTime.UtcNow),
                    new JProperty("requestId", context.RequestId),
                    new JProperty("error", context.LastError?.Reason ?? "Unknown error"),
                    new JProperty("statusCode", context.Response?.StatusCode),
                    new JProperty("method", context.Request?.Method),
                    new JProperty("url", context.Request?.Url?.ToString())
                ).ToString();
            }</message>
        </trace>
        
        <!-- Return standardized error response -->
        <return-response>
            <set-status code="500" />
            <set-header name="Content-Type" exists-action="override">
                <value>application/json</value>
            </set-header>
            <set-body>@{
                return JsonConvert.SerializeObject(new {
                    error = "Internal server error",
                    message = "An error occurred while processing your request",
                    requestId = context.RequestId,
                    timestamp = DateTime.UtcNow.ToString("yyyy-MM-ddTHH:mm:ssZ")
                });
            }</set-body>
        </return-response>
    </on-error>
</policies> 